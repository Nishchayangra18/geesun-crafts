export default function AccountPage() {
  return (
    <section className="container-shell section-space">
      <div className="card-surface p-6">
        <h1 className="font-[var(--font-heading)] text-5xl">Account</h1>
        <p className="mt-3 text-sm text-[var(--text-muted)]">
          Account dashboard setup is ready. Next phase can include order history, address book, and profile settings.
        </p>
      </div>
    </section>
  );
}
